import { ProjectionSphereModel } from '../projection-sphere-model';

describe('ProjectionSphereModel', () => {
  it('should create an instance', () => {
    expect(new ProjectionSphereModel()).toBeTruthy();
  });
});
